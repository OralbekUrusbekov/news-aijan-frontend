services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: news-aijan-frontend:local
    container_name: news-aijan-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
